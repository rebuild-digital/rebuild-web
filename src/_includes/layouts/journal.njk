---
layout: layouts/base.njk
---

{% block content %}
<div class="container">
  <section class="journal-listing pt-xl pb-2xl">
    <header class="mb-xl">
      <h1>{{ title | default("Journal") }}</h1>
      {% if description %}
        <div class="rich-text">
          {{ description | safe }}
        </div>
      {% endif %}
    </header>

    {{ content | safe }}
  </section>
</div>
{% endblock %}
