{#
  Card component
  Usage: {% include "components/card.njk" %}

  Parameters:
  - title: Card title
  - description: Card description
  - image: Image URL (optional)
  - link: Link URL (optional)
  - tags: Array of tags (optional)
#}

<article class="overflow-hidden transition-all duration-base">
  {% if image %}
    <div>
      <a href="{{ link }}">
        <img src="{{ image }}" alt="{{ title }}" loading="lazy" class="w-full object-cover">
      </a>
    </div>
  {% endif %}

  <div class="mt-md">
    {% if title %}
      <h3 class="text-xl lg:text-3xl mb-sm">
        {% if link %}
          <a href="{{ link }}" class="text-dark no-underline hover:underline">{{ title }}</a>
        {% else %}
          {{ title }}
        {% endif %}
      </h3>
    {% endif %}

    {% if author %}
      <p class="text-dark">— {{ author }}</p>
    {% endif %}

    {# {% if tags and tags.length > 0 %}
      <div class="flex gap-xs flex-wrap">
        {% for tag in tags %}
          <span class="inline-block px-sm py-xs border border-dark text-xs font-semibold">{{ tag }}</span>
        {% endfor %}
      </div>
    {% endif %} #}
  </div>
</article>